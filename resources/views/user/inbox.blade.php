@extends('layouts.app')

@section('content')
    <div class="my-places">
        <div class="container">
            {{--@foreach ($messages as $message)
               <h3>Message from: {{ $message->guest_name }}</h3>
               <h4>{{ $message->mail_address }}</h4>
               <h2>{{ $message->subject }}</h2>
               <p>{{ $message->subject }}</p>
            @endforeach--}}
        </div>
    </div>
@endsection